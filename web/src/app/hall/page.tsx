import HallBoot from "@/components/HallBoot";

// Every box ever opened, plus the totals the contract holds. Read-only, no wallet needed.
export default function HallPage() {
  return <HallBoot />;
}
